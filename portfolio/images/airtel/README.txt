This folder holds the images for "Intermission // Airtel" (airtel.html).

Filenames are fixed slots, not content-derived - drop a same-named
.png in here and it appears automatically, no code edits needed.
Anything missing is simply skipped (the box shows its plain
placeholder background instead), so nothing breaks in the meantime.
Recommended: export at 1600px on the long edge, PNG. PNGs run
larger than JPGs - if a file's much over ~1-2MB, run it through an
optimizer (e.g. TinyPNG/Squoosh) before dropping it in.

Expected filenames, in the order they appear on the page top to
bottom:

00-cover.png  <- cover / hero image
01.png        <- near: "Intermission is a podcast series with dedicated companion websites, each episode tracin..."
02.png        <- section "SCRIPT" — near: "The process started with the script but needed additional data."
03.png        <- section "EXPLORATIONS" — labelled "Family Tree"
04.png        <- section "EXPLORATIONS" — labelled "Stocks Chart"
05.png        <- section "DESIGN SYSTEM" — near: "The site's visual language is owned by our visual team."
06.png        <- section "DESIGN SYSTEM" — near: "Claude Code helps encapsulate all the single HTMLs created by Google Gemini into a sing..."
07.png        <- section "DESIGN SYSTEM" — near: "Claude Code helps encapsulate all the single HTMLs created by Google Gemini into a sing..."
08.png        <- section "DESIGN SYSTEM" — near: "Claude Code helps encapsulate all the single HTMLs created by Google Gemini into a sing..."
09.png        <- section "DESIGN SYSTEM" — near: "Claude Code helps encapsulate all the single HTMLs created by Google Gemini into a sing..."
10.png        <- section "DESIGN SYSTEM" — near: "Claude Code helps encapsulate all the single HTMLs created by Google Gemini into a sing..."
11.png        <- section "Q&A" — near: "We used a Vercel server to push visualisations in real time, which let the wider team —..."
12.png        <- section "Q&A" — near: "We used a Vercel server to push visualisations in real time, which let the wider team —..."
13.gif        <- section "INTEGRATION" — near: "We used a Vercel server to push visualisations in real time, which let the wider team —..." (animated, not a .png - matches project.html's 13.gif)
14.png        <- section "INTEGRATION" — near: "We used GitHub for version control, which kept our work transparent and kept us both ac..."
15.png        <- section "INTEGRATION" — near: "We used GitHub for version control, which kept our work transparent and kept us both ac..."
16.png        <- section "INTEGRATION" — near: "We used GitHub for version control, which kept our work transparent and kept us both ac..."
17.png        <- section "INTEGRATION" — near: "We used GitHub for version control, which kept our work transparent and kept us both ac..."

(The BACK/NEXT pager cards at the very bottom of the page show
undersea-cables.html's and lightrock.html's own 00-cover.png instead
of a slot from this folder.)

This folder holds the images for "Undersea Cables" (undersea-cables.html).

Filenames are fixed slots, not content-derived - drop a same-named
.png in here and it appears automatically, no code edits needed.
Anything missing is simply skipped (the box shows its plain
placeholder background instead), so nothing breaks in the meantime.
Recommended: export at 1600px on the long edge, PNG. PNGs run
larger than JPGs - if a file's much over ~1-2MB, run it through an
optimizer (e.g. TinyPNG/Squoosh) before dropping it in.

Expected filenames, in the order they appear on the page top to
bottom:

00-cover.png  <- cover / hero image
01.png        <- near: ""India's internet runs on cables it cannot protect, repair, or regulate — in spite of k..."
02.png        <- section "DATA" — near: "Showing the full network — not just the one cable in the headline — was essential for t..."
03.png        <- section "DATA" — near: "Showing the full network — not just the one cable in the headline — was essential for t..."
04.png        <- section "DESIGN" — near: "Once the data format was understood, I rebuilt it in a working spreadsheet."
05.png        <- section "DESIGN" — near: "The base visualisation was generated with Google Gemini and JavaScript libraries."
06.png        <- section "DESIGN" — near: "With the functional visualisation in place, I built out the visual design in Figma."

(The BACK/NEXT pager cards at the very bottom of the page show
project.html's and airtel.html's own 00-cover.png instead of a
slot from this folder.)

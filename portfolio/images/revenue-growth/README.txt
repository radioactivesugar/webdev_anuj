This folder holds the images for "Revenue Growth (NBFC)" (revenue-growth.html).

Filenames are fixed slots, not content-derived - drop a same-named
.png in here and it appears automatically, no code edits needed.
Anything missing is simply skipped (the box shows its plain
placeholder background instead), so nothing breaks in the meantime.
Recommended: export at 1600px on the long edge, PNG. PNGs run
larger than JPGs - if a file's much over ~1-2MB, run it through an
optimizer (e.g. TinyPNG/Squoosh) before dropping it in.

Expected filenames, in the order they appear on the page top to
bottom:

00-cover.png  <- cover / hero image
01.png        <- near: ""The very first application came back sanctioned by an RBI-licensed NBFC, at an annuali..."
02.png        <- section "DATA" — near: "Secondary Layerof data"
03.png        <- section "DESIGN" — near: "Secondary Layerof data"
04.png        <- section "DESIGN" — near: "The result is a packed bubble chart, titled Class of FY25, clustering NBFCs whose high-..."
05.png        <- section "DESIGN" — near: "Hover state — surfaces company-level detail"

(The BACK pager card at the very bottom of the page shows
lightrock.html's own 00-cover.png instead of a slot from this
folder. There's no NEXT card - this is the last episode.)
